.env
/secrets.yaml
/secrets.json
.tmp/

# NixOS secrets
# - Root secrets.json: plaintext source (gitignored)
# - secrets.nix files: auto-generated, tracked in git (with placeholder values for flake check)
nix/machines/*/local.nix
# WiFi connection files (contain passwords)
**/*.nmconnection
# Nix build results (symlinks created by nix build)
**/result
result
